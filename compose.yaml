services:
  app:
    build:
      context: .
      target: development
    ports:
      - 3000:80
    environment:
      - PORT=80
    develop:
      watch:
        - path: ./src
          target: /app/src
          action: sync
    networks:
      - config-store-net

networks:
  config-store-net:
